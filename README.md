# GCP Pub Sub test

- `main.py` to run the Flask dev server
- `subscriber.py` to run the pub sub subscriber that pulls the messages from pub sub and sends them to `/cmdb` route

![Pub Sub image](/img/pub_sub.png)
